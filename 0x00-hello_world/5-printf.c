#include <stdio.h>

/**
 * main - This program prints a sentence followed by a new line, using printf.
 *
 * Return: Always (0).
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

		return (0);
}
