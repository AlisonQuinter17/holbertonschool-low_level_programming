#include "holberton.h"

/**
 * _abs - main function.
 * @i: Test character.
 *
 * Description: This function computes the absolute value of an integer.
 * Return: 0.
 */

int _abs(int i)
{
	return (i > 0 ? i : -i);
}
